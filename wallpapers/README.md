# Wallpapers

Add your wallpapers to this directory. The install script will symlink them
to `~/Pictures/Wallpapers/`.

This directory is gitignored — wallpapers are excluded from version control
due to their large size. Only this README is tracked.
